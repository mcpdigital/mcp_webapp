import React from "react";
import PropTypes from "prop-types";
import { GRAD_GOTHAN } from "../../tw_gradients";

const API = (props) => {
  return <div className={GRAD_GOTHAN + " p-4"}>API</div>;
};

API.propTypes = {};

export default API;
